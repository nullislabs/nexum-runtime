# fuel-bomb test fixture. Subscribes to a single chain's
# blocks so the supervisor invokes `on_event` once; the unbounded
# loop in `on_event` then exhausts the wasmtime fuel budget and the
# host traps `OutOfFuel`. The integration test asserts the trap is
# caught + the module is marked dead.

[module]
name = "fuel-bomb"
version = "0.1.0"

[capabilities]
required = ["logging"]

[capabilities.http]
allow = []

[[subscription]]
kind = "block"
chain_id = 1

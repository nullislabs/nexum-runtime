# The pinned-digest fixture is hashed byte-for-byte against the digest in
# its sibling module.toml, so git must never rewrite its line endings.
crates/nexum-runtime/testdata/pinned/component.wat -text
